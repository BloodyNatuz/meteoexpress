<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MeteoExpress - {{ pageName }}</title>
</head>
<body>
    <nav>
        <a href="/">Accueil</a>
    </nav>
    <main>
        <h1>{{ message }}</h1>

        <h2>{{ villeName }}</h2>
        <p>({{villeLon}} ; {{villeLat}})</p>

        <img src="http://localhost:4000/img/{{villeWeather}}.svg" alt="{{villeWeather}}">
        <p>{{villeTemp}}°C</p>
    </main>
</body>
</html>